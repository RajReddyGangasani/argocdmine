apiversion: apps/v1
kind: Deployment
metadata:
  name: mydeploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: swiggy
  template:
    metadata:
      labels:
        app: swiggy
    spec: 
      container:
        - name: cont1
          image: shaikmustafa/dm
          ports:
            - containerPort: 80
